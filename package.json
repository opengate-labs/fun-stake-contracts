{
  "name": "fun-stake-contract",
  "version": "1.0.0",
  "license": "(MIT AND Apache-2.0)",
  "type": "module",
  "scripts": {
    "build": "near-sdk-js build src/v2_contract.ts build/v2_contract.wasm",
    "test": "$npm_execpath run build && ava -- ./build/v2_contract.wasm"
  },
  "dependencies": {
    "near-sdk-js": "1.0.0"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.8.3",
    "ava": "^6.1.3",
    "near-workspaces": "^3.5.0",
    "typescript": "^5.4.5"
  },
  "ava": {
    "files": [
      "sandbox-test/*.ava.js"
    ],
    "timeout": "1000s"
  }
}
